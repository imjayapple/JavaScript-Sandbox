// Real life example

/*
Properties
car.name = Honda
car.model = CRV
car.weight = 1200lbs

Methods
car.start()
car.drive()
car.brake()
car.stop()
*/

// Objects are variables too, but objects can contain many values
const car = {type: "Fiat", model: "500", color: "white"};

// ↑ These values are written as 'name:value' pairs

// Objects also have methods
console.log(Math.random()); // This will print a random number between 0 & 1

// recall: the random number is between 0 (inclusive) and 1 (exclusive)

console.log(Math.floor(Math.random() * 30)); // Print a random whole number between 0 & 30
